{
  "name": "test-release-process",
  "version": "1.0.0",
  "description": "```bash $ git checkout --detach # run `npm run build` to generate the `dist` directory $ git commit -m 1.0.0 $ git tag v1.0.0 $ git push --tags ```",
  "main": "index.js",
  "scripts": {
    "build": "mkdir dist && touch ./dist/finn.dist",
    "release": "./tools/release.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ParkSB/test-release-process.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ParkSB/test-release-process/issues"
  },
  "homepage": "https://github.com/ParkSB/test-release-process#readme"
}
